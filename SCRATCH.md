# Scratch Artifacts

Tracks notable artifacts on `/work/scratch/<user>/...`. Scratch dirs are world-readable, so anyone on the cluster can `rsync`/`cp` directly. Auto-deletion: see `CLUSTER.md`.

> When adding a new artifact, also `rsync` a critical-only backup to your laptop before relying on it (cluster scratch is not durable).

| Baseline | Path | Owner | Description |
|---|---|---|---|
| Baseline 1 (DA3MONO-LARGE zero-shot) | `/work/scratch/nmeurer/outputs/baseline1` | nmeurer | Final zero-shot student predictions and submission CSV. Ready to submit. Contains `preds/*.npy`, `depth_vis/*.png`, `baseline1_submission.csv`. |
| Baseline 3 teacher pseudo-labels (`DA3-GIANT-1.1`) | `/work/scratch/nmeurer/outputs/baseline3/pseudo_labels_DA3-GIANT-1.1` | nmeurer | Teacher pseudo-labels over the train set, generated by `DA3-GIANT-1.1` via `src/bs3_pseudo_label_DA3.py`. 22,605 `*_depth.npy` files (float32, 560×560, invalid=0). Used as student fine-tuning targets. |
| Baseline 3 teacher pseudo-labels (`DA3NESTED-GIANT-LARGE-1.1`) | `/work/scratch/mdealvaro/outputs/baseline3/pseudo_labels_DA3NESTED-GIANT-LARGE-1.1` | mdealvaro | Teacher pseudo-labels over the train set, generated by `DA3NESTED-GIANT-LARGE-1.1`. Same format as above. |
